extends layout

block content
    h1= title
    - let image_names = ['cardio.webp','dentist.jpeg','derma.webp','radio.jpg','famMeds.webp']
    - for (let i=0; i<3; i++){
        - let num = Math.floor(Math.random()*5);
        img(src=`\\images\\${image_names[num]}` alt=`This is image ${num}` height="300" width="300")
    - }
